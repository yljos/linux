[Unit]
Description=Telegram Bot Service (Podman)
After=network-online.target

[Service]
Type=simple
Restart=always
RestartSec=5
# No logs in systemd
StandardOutput=null
StandardError=null

ExecStart=/usr/bin/podman run \
  --name bot \
  --replace \
  --rm \
  --init \
  --log-driver=none \
  -p 5005:5005 \
  --env-file /root/bot/.env \
  -v /root/bot/blocked_users.json:/app/blocked_users.json:Z \
  -v /root/bot/whitelist.json:/app/whitelist.json:Z \
  localhost/bot:latest

[Install]
WantedBy=multi-user.target

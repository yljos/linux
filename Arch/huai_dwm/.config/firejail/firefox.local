# 保留现有黑名单
blacklist ${HOME}/下载
blacklist ${HOME}/Documents
blacklist ${HOME}/Desktop

# 传递 Firefox 启动参数
args --ignore-gpu-blacklist --enable-vaapi

# 或者用环境变量方式（更稳定）
env MOZ_X11_EGL=1
env MOZ_ENABLE_WAYLAND=1
env MOZ_ACCELERATED=1
env MOZ_USE_HWDEC=1


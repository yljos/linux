[Unit]
Description=Vaultwarden Container
Wants=network-online.target
After=network-online.target

[Service]
Type=simple
Restart=always
RestartSec=5s

ExecStart=/usr/bin/podman run \
  --replace \
  --rm \
  --init \
  --name=pw \
  --log-driver=journald \
  -e TZ=Asia/Shanghai \
  -p 8181:80 \
  -v /data/appdata/vaultwarden:/data:Z \
  docker.io/vaultwarden/server:latest

ExecStop=/usr/bin/podman stop -t 10 pw

[Install]
WantedBy=default.target
